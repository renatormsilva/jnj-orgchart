# ===========================================
# J&J OrgChart API - Environment Variables
# ===========================================

# Application
NODE_ENV=development
PORT=3000
HOST=0.0.0.0

# Database (PostgreSQL)
# Option 1: Local PostgreSQL (with Docker)
# DATABASE_URL="postgresql://postgres:postgres@localhost:5432/jj_orgchart?schema=public"

# Option 2: Supabase (recommended for quick setup)
# Get your connection string from: Supabase Dashboard > Project Settings > Database > Connection string > URI
# Note: If your password contains @ symbol, encode it as %40
DATABASE_URL="postgresql://postgres:YOUR_PASSWORD@db.xxxxx.supabase.co:5432/postgres"

# Logging
LOG_LEVEL=info

# CORS
CORS_ORIGIN=http://localhost:5173

# API
API_PREFIX=/api/v1

# Authentication (API Key)
# Set to 'true' to enable API key authentication for write operations
API_KEY_ENABLED=false
# Generate a secure API key (e.g., using: openssl rand -hex 32)
API_KEY=your-secure-api-key-here
